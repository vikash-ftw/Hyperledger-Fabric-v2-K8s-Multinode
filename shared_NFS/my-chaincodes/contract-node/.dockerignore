# dependencies
node_modules

package-lock.json

# packaging directory
packaging

# Build outputs (if building in container)
dist
build

# Git
.git
.gitignore

# Docker files not needed in context
Dockerfile*
docker-compose*
.dockerignore

# tar files
*.gz
*.tgz
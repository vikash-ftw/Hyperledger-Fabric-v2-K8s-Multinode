apiVersion: v1
kind: Service
metadata:
  name: explorer-svc
  labels:
    app: explorer
spec:
  type: NodePort
  selector:
    app: hyperledger-explorer
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      nodePort: 30080